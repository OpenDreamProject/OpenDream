
/proc/RunTest()
	ASSERT(global.vars["UNIX"] == UNIX)
	ASSERT(global.vars["NORTH"] == NORTH)
	ASSERT(global.vars["SOUTH"] == SOUTH)
	ASSERT(global.vars["EAST"] == EAST)
	ASSERT(global.vars["WEST"] == WEST)
	ASSERT(global.vars["NORTHEAST"] == NORTHEAST)
	ASSERT(global.vars["NORTHWEST"] == NORTHWEST)
	ASSERT(global.vars["SOUTHEAST"] == SOUTHEAST)
	ASSERT(global.vars["SOUTHWEST"] == SOUTHWEST)
	ASSERT(global.vars["UP"] == UP)
	ASSERT(global.vars["DOWN"] == DOWN)
	ASSERT(global.vars["BLIND"] == BLIND)
	ASSERT(global.vars["SEE_MOBS"] == SEE_MOBS)
	ASSERT(global.vars["SEE_OBJS"] == SEE_OBJS)
	ASSERT(global.vars["SEE_TURFS"] == SEE_TURFS)
	ASSERT(global.vars["SEE_SELF"] == SEE_SELF)
	ASSERT(global.vars["SEE_INFRA"] == SEE_INFRA)
	ASSERT(global.vars["SEE_PIXELS"] == SEE_PIXELS)
	ASSERT(global.vars["SEE_THRU"] == SEE_THRU)
	ASSERT(global.vars["SEE_BLACKNESS"] == SEE_BLACKNESS)
	ASSERT(global.vars["MOB_PERSPECTIVE"] == MOB_PERSPECTIVE)
	ASSERT(global.vars["EYE_PERSPECTIVE"] == EYE_PERSPECTIVE)
	ASSERT(global.vars["EDGE_PERSPECTIVE"] == EDGE_PERSPECTIVE)
	ASSERT(global.vars["FLOAT_LAYER"] == FLOAT_LAYER)
	ASSERT(global.vars["AREA_LAYER"] == AREA_LAYER)
	ASSERT(global.vars["TURF_LAYER"] == TURF_LAYER)
	ASSERT(global.vars["OBJ_LAYER"] == OBJ_LAYER)
	ASSERT(global.vars["MOB_LAYER"] == MOB_LAYER)
	ASSERT(global.vars["FLY_LAYER"] == FLY_LAYER)
	ASSERT(global.vars["EFFECTS_LAYER"] == EFFECTS_LAYER)
	ASSERT(global.vars["TOPDOWN_LAYER"] == TOPDOWN_LAYER)
	ASSERT(global.vars["BACKGROUND_LAYER"] == BACKGROUND_LAYER)
	ASSERT(global.vars["FLOAT_PLANE"] == FLOAT_PLANE)
	ASSERT(global.vars["TOPDOWN_MAP"] == TOPDOWN_MAP)
	ASSERT(global.vars["ISOMETRIC_MAP"] == ISOMETRIC_MAP)
	ASSERT(global.vars["SIDE_MAP"] == SIDE_MAP)
	ASSERT(global.vars["TILED_ICON_MAP"] == TILED_ICON_MAP)
	ASSERT(global.vars["TRUE"] == TRUE)
	ASSERT(global.vars["FALSE"] == FALSE)
	ASSERT(global.vars["MALE"] == MALE)
	ASSERT(global.vars["FEMALE"] == FEMALE)
	ASSERT(global.vars["NEUTER"] == NEUTER)
	ASSERT(global.vars["PLURAL"] == PLURAL)
	ASSERT(global.vars["MOUSE_INACTIVE_POINTER"] == MOUSE_INACTIVE_POINTER)
	ASSERT(global.vars["MOUSE_ACTIVE_POINTER"] == MOUSE_ACTIVE_POINTER)
	ASSERT(global.vars["MOUSE_DRAG_POINTER"] == MOUSE_DRAG_POINTER)
	ASSERT(global.vars["MOUSE_DROP_POINTER"] == MOUSE_DROP_POINTER)
	ASSERT(global.vars["MOUSE_ARROW_POINTER"] == MOUSE_ARROW_POINTER)
	ASSERT(global.vars["MOUSE_CROSSHAIRS_POINTER"] == MOUSE_CROSSHAIRS_POINTER)
	ASSERT(global.vars["MOUSE_HAND_POINTER"] == MOUSE_HAND_POINTER)
	ASSERT(global.vars["MOUSE_LEFT_BUTTON"] == MOUSE_LEFT_BUTTON)
	ASSERT(global.vars["MOUSE_RIGHT_BUTTON"] == MOUSE_RIGHT_BUTTON)
	ASSERT(global.vars["MOUSE_MIDDLE_BUTTON"] == MOUSE_MIDDLE_BUTTON)
	ASSERT(global.vars["MOUSE_CTRL_KEY"] == MOUSE_CTRL_KEY)
	ASSERT(global.vars["MOUSE_SHIFT_KEY"] == MOUSE_SHIFT_KEY)
	ASSERT(global.vars["MOUSE_ALT_KEY"] == MOUSE_ALT_KEY)
	ASSERT(global.vars["MS_WINDOWS"] == MS_WINDOWS)
	ASSERT(global.vars["SOUND_MUTE"] == SOUND_MUTE)
	ASSERT(global.vars["SOUND_PAUSED"] == SOUND_PAUSED)
	ASSERT(global.vars["SOUND_STREAM"] == SOUND_STREAM)
	ASSERT(global.vars["SOUND_UPDATE"] == SOUND_UPDATE)
	ASSERT(global.vars["BLEND_DEFAULT"] == BLEND_DEFAULT)
	ASSERT(global.vars["BLEND_OVERLAY"] == BLEND_OVERLAY)
	ASSERT(global.vars["BLEND_ADD"] == BLEND_ADD)
	ASSERT(global.vars["BLEND_SUBTRACT"] == BLEND_SUBTRACT)
	ASSERT(global.vars["BLEND_MULTIPLY"] == BLEND_MULTIPLY)
	ASSERT(global.vars["BLEND_INSET_OVERLAY"] == BLEND_INSET_OVERLAY)
