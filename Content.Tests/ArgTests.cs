using NUnit.Framework;
using OpenDreamRuntime;
using OpenDreamRuntime.Procs;

namespace Content.Tests
{
    [TestFixture]
    public partial class DMTests
    {
        // TODO Failing test. DM doesn't compile
        /*[Test]
        public void ArgEquals1() {
            var prev = _dreamMan.DMExceptionCount;

            var result = DreamThread.Run(async(state) => {
                var world = _dreamMan.WorldInstance;
                var proc = world.GetProc("ArgEquals1_proc");
                return await state.Call(proc, world, null, new DreamProcArguments(null));
            });

            Assert.That(_dreamMan.DMExceptionCount, Is.EqualTo(prev));
        }*/

        // TODO Failing test. DM doesn't compile
        /*[Test]
        public void ArgEquals2() {
            var prev = _dreamMan.DMExceptionCount;

            var result = DreamThread.Run(async(state) => {
                var world = _dreamMan.WorldInstance;
                var proc = world.GetProc("ArgEquals2_proc");
                return await state.Call(proc, world, null, new DreamProcArguments(null));
            });

            Assert.That(_dreamMan.DMExceptionCount, Is.EqualTo(prev));
        }*/

        // TODO Failing test. DM doesn't compile
        /*[Test]
        public void ArgEquals3() {
            var prev = _dreamMan.DMExceptionCount;

            var result = DreamThread.Run(async(state) => {
                var world = _dreamMan.WorldInstance;
                var proc = world.GetProc("ArgEquals3_proc");
                return await state.Call(proc, world, null, new DreamProcArguments(null));
            });

            Assert.That(_dreamMan.DMExceptionCount, Is.EqualTo(prev));
        }*/

        // TODO Failing test. DM doesn't compile
        /*[Test]
        public void ArgProcsProcs1() {
            var prev = _dreamMan.DMExceptionCount;

            var result = DreamThread.Run(async(state) => {
                var world = _dreamMan.WorldInstance;
                var proc = world.GetProc("ArgProcsProcs1_proc");
                return await state.Call(proc, world, null, new DreamProcArguments(null));
            });

            Assert.That(_dreamMan.DMExceptionCount, Is.EqualTo(prev));
        }*/
    }
}
