
uniform highp float x;
uniform highp float y;   
uniform highp float flags;
uniform sampler2D mask_texture;

void fragment() {
    highp vec4 new_color = texture(TEXTURE, UV);
    new_color.a *= texture(mask_texture, UV).a; // D

    COLOR = new_color;
}